name: backend-ci
on: [push, pull_request]
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: arcscrow
          POSTGRES_PASSWORD: test
          POSTGRES_DB: arcscrow_test
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U arcscrow" --health-interval 5s
          --health-timeout 5s --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.12", cache: pip}
      - run: pip install -e '.[dev]'
      - run: ruff check .
      - run: mypy app
      - run: pytest --cov=app
      - run: alembic upgrade head
        env:
          DATABASE_URL: postgresql+asyncpg://arcscrow:test@localhost:5432/arcscrow_test
          SESSION_SECRET: ci-session-secret-that-is-long-enough
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build .
